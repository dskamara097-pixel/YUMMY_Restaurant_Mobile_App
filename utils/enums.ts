export enum UserRoleEnum {
  Customer = 'customer',
  Vendor = 'vendor',
  Admin = 'admin',
  Rider = 'rider',
}

export enum OrderStatusEnum {
  Pending = 'pending',
  PaymentReceived = 'paymentReceived',
  Preparing = 'preparing',
  Ready = 'ready',
  Delivered = 'delivered',
  Cancelled = 'cancelled',
}
